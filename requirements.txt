# NeuraCoin Python Dependencies
# Pinned versions for reproducible builds across CI and local development.
# Last reviewed: 2026-04-12

# --- Ethereum / Web3 ---
web3==6.20.1
eth-account==0.11.2
eth-utils==4.1.1
eth-abi==5.1.0
hexbytes==0.3.1

# --- Smart contract tooling (hardhat-shim style helper) ---
# eth-brownie is unmaintained for newer solc/web3; we use py-solc-x for compilation
# and rely on Hardhat (Node) for the full toolchain. py-solc-x lets Python tests
# compile Solidity sources directly when needed.
py-solc-x==2.0.3

# --- CLI ---
click==8.1.7
rich==13.7.1
python-dotenv==1.0.1

# --- HTTP / JSON (used by node client and CLI) ---
requests==2.32.3
urllib3==2.2.2
httpx==0.27.0
orjson==3.10.7

# --- Async / networking (node client polling loop) ---
aiohttp==3.9.5
tenacity==8.5.0

# --- Testing ---
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-mock==3.14.0
pytest-cov==5.0.0
responses==0.25.3

# --- Typing / lint support ---
typing-extensions==4.12.2
